\label{index_md_README}%
\Hypertarget{index_md_README}%
 \href{https://github.com/Timmifixedit/IteratorTools/actions/workflows/unit_tests.yml}{\texttt{ }} \hypertarget{index_autotoc_md0}{}\doxysubsection{Python-\/like Zip and Enumerate Iterators}\label{index_autotoc_md0}
C++-\/implementation of Python-\/like zip-\/ and enumerate-\/iterators which can be used in range-\/based for loops along with structured bindings to iterate over multiple containers at the same time. Requires C++17.\hypertarget{index_autotoc_md1}{}\doxysubsubsection{Properties}\label{index_autotoc_md1}
The {\ttfamily zip}-\/class is a container-\/wrapper for arbitrary iterable containers. It provides the member functions {\ttfamily begin()} and {\ttfamily end()} enabling it to be used in range-\/based for loops to iterate over multiple containers at the same time. The {\ttfamily enuerate}-\/function is a special case of {\ttfamily zip} and uses a \char`\"{}counting container\char`\"{} (similar to {\ttfamily std\+::ranges\+::iota}) to provide an index. Additionally, const-\/versions exist which do not allow the manipulation of the container elements.\hypertarget{index_autotoc_md2}{}\doxysubsubsection{Doxygen Documentation}\label{index_autotoc_md2}

\begin{DoxyItemize}
\item \href{https://timmifixedit.github.io/IteratorTools/html/index.html}{\texttt{ HTML}}
\item \href{https://timmifixedit.github.io/IteratorTools/ZipEnumerateCppDocs.pdf}{\texttt{ PDF}}
\end{DoxyItemize}\hypertarget{index_autotoc_md3}{}\doxysubsubsection{Code Examples}\label{index_autotoc_md3}
The syntax is mostly similar to Python\+: 
\begin{DoxyCode}{0}
\DoxyCodeLine{\textcolor{preprocessor}{\#include <vector>}}
\DoxyCodeLine{\textcolor{preprocessor}{\#include <list>}}
\DoxyCodeLine{\textcolor{preprocessor}{\#include "{}\mbox{\hyperlink{Iterators_8hpp}{Iterators.hpp}}"{}}}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{keyword}{using namespace }\mbox{\hyperlink{namespaceiterators}{iterators}};}
\DoxyCodeLine{std::list<std::string> strings\{\textcolor{stringliteral}{"{}a"{}}, \textcolor{stringliteral}{"{}b"{}}, \textcolor{stringliteral}{"{}c"{}}\};}
\DoxyCodeLine{std::vector<int> numbers\{1, 2, 3\};}
\DoxyCodeLine{\textcolor{keywordflow}{for} (\textcolor{keyword}{auto} [\textcolor{keywordtype}{string}, number] : \mbox{\hyperlink{namespaceiterators_a420098bf63d0d2600a2c5afdf6602f62}{zip}}(strings, numbers)) \{}
\DoxyCodeLine{    \textcolor{comment}{// 'string' and 'number' are references to the container element}}
\DoxyCodeLine{    \textcolor{keywordtype}{string} += std::to\_string(number); }
\DoxyCodeLine{\}}
\DoxyCodeLine{}
\DoxyCodeLine{\textcolor{comment}{// now 'strings' contains \{"{}a1"{}, "{}b2"{}, "{}c3"{}\}}}

\end{DoxyCode}
 The for loop uses so called {\ttfamily Zip\+Iterator}s which point to tuples which in turn contain references to the container elements. Therefore, no copying occurs and manipulation of the container elements is possible. Observe that the structured binding captures by value (since the values are themselves references).

If you want to prohibit manipulation, you can use {\ttfamily const\+\_\+zip} 
\begin{DoxyCode}{0}
\DoxyCodeLine{\textcolor{keyword}{using namespace }\mbox{\hyperlink{namespaceiterators}{iterators}};}
\DoxyCodeLine{std::list<std::string> strings\{\textcolor{stringliteral}{"{}a"{}}, \textcolor{stringliteral}{"{}b"{}}, \textcolor{stringliteral}{"{}c"{}}\};}
\DoxyCodeLine{std::vector<int> numbers\{1, 2, 3\};}
\DoxyCodeLine{\textcolor{keywordflow}{for} (\textcolor{keyword}{auto} [\textcolor{keywordtype}{string}, number] : \mbox{\hyperlink{namespaceiterators_a12e6f5a7c31da947017d513b58e57dd4}{const\_zip}}(strings, numbers)) \{}
\DoxyCodeLine{    \textcolor{comment}{// string += std::to\_string(number);  error, string is readonly!}}
\DoxyCodeLine{    std::cout << \textcolor{keywordtype}{string} << \textcolor{stringliteral}{"{} "{}} << number << std::endl;}
\DoxyCodeLine{\}}

\end{DoxyCode}
 Additionally, you can use {\ttfamily zip\+\_\+i} to manually zip iterators or pointers\+: 
\begin{DoxyCode}{0}
\DoxyCodeLine{\textcolor{keyword}{using namespace }\mbox{\hyperlink{namespaceiterators}{iterators}};}
\DoxyCodeLine{std::list<std::string> strings\{\textcolor{stringliteral}{"{}a"{}}, \textcolor{stringliteral}{"{}b"{}}, \textcolor{stringliteral}{"{}c"{}}\};}
\DoxyCodeLine{std::vector<int> numbers\{1, 2, 3\};}
\DoxyCodeLine{\textcolor{keyword}{auto} zipBegin = \mbox{\hyperlink{namespaceiterators_acd7ca481d3b479c30879e611baaac1da}{zip\_i}}(strings.begin(), numbers.begin());}
\DoxyCodeLine{\textcolor{keyword}{auto} zipEnd = \mbox{\hyperlink{namespaceiterators_acd7ca481d3b479c30879e611baaac1da}{zip\_i}}(strings.end(), numbers.end());}
\DoxyCodeLine{\textcolor{keywordflow}{while} (zipBegin != zipEnd) \{}
\DoxyCodeLine{    \textcolor{keyword}{auto} [s, num] = *zipBegin;}
\DoxyCodeLine{    \textcolor{comment}{// ...}}
\DoxyCodeLine{    ++zipBegin;}
\DoxyCodeLine{\}}

\end{DoxyCode}
 {\ttfamily Zip\+Iterators} support the same operations as the least powerful underlying iterator. For example, if you zip a random access iterator (e.\+g. from {\ttfamily std\+::vector}) and a bidirectional iterator (e.\+g. from {\ttfamily std\+::list}), then the resulting {\ttfamily Zip\+Iterator} will only support bidirectional iteration but no random access.

As in Python, the shortest range decides the overall range\+: 
\begin{DoxyCode}{0}
\DoxyCodeLine{\textcolor{keyword}{using namespace }\mbox{\hyperlink{namespaceiterators}{iterators}};}
\DoxyCodeLine{std::list<std::string> strings\{\textcolor{stringliteral}{"{}a"{}}, \textcolor{stringliteral}{"{}b"{}}, \textcolor{stringliteral}{"{}c"{}}\};}
\DoxyCodeLine{std::vector<int> numbers\{1, 2, 3, 4, 5, 6\};}
\DoxyCodeLine{\textcolor{keywordflow}{for} (\textcolor{keyword}{auto} [\textcolor{keywordtype}{string}, number] : \mbox{\hyperlink{namespaceiterators_a420098bf63d0d2600a2c5afdf6602f62}{zip}}(strings, numbers)) \{}
\DoxyCodeLine{    std::cout << \textcolor{keywordtype}{string} << \textcolor{stringliteral}{"{} "{}} << number << \textcolor{stringliteral}{"{} | "{}}}
\DoxyCodeLine{\}}
\DoxyCodeLine{\textcolor{comment}{// prints a 1 | b 2 | c 3 |}}

\end{DoxyCode}


The {\ttfamily enumerate}-\/function works similarly. 
\begin{DoxyCode}{0}
\DoxyCodeLine{\textcolor{keyword}{using namespace }\mbox{\hyperlink{namespaceiterators}{iterators}};}
\DoxyCodeLine{std::list<std::string> strings\{\textcolor{stringliteral}{"{}a"{}}, \textcolor{stringliteral}{"{}b"{}}, \textcolor{stringliteral}{"{}c"{}}\};}
\DoxyCodeLine{\textcolor{keywordflow}{for} (\textcolor{keyword}{auto} [index, \textcolor{keywordtype}{string}] : \mbox{\hyperlink{namespaceiterators_aaea70262053f925d78710baa65075124}{enumerate}}(strings)) \{}
\DoxyCodeLine{    \textcolor{keywordtype}{string} += std::to\_string(index); }
\DoxyCodeLine{\}}
\DoxyCodeLine{\textcolor{comment}{// now 'strings' contains \{"{}a0"{}, "{}b1"{}, "{}c2"{}\}}}

\end{DoxyCode}
 Also, an optional offset can be specified\+: 
\begin{DoxyCode}{0}
\DoxyCodeLine{\textcolor{keywordflow}{for} (\textcolor{keyword}{auto} [index, \textcolor{keywordtype}{string}] : \mbox{\hyperlink{structiterators_1_1impl_1_1ZipView_a4c8c6849391d6b4576cb63ec05a725f3}{enumerate}}(strings, 4)) \{ \textcolor{comment}{// index starts from 4}}
\DoxyCodeLine{    ...}
\DoxyCodeLine{\}}

\end{DoxyCode}
 And as with {\ttfamily zip}, a const version ({\ttfamily const\+\_\+enumerate}) exists.

In case temporary containers are used, {\ttfamily zip} and {\ttfamily enumerate} will take ownership of the containers to guarantee well-\/defined memory access. 
\begin{DoxyCode}{0}
\DoxyCodeLine{\textcolor{keywordflow}{for} (\textcolor{keyword}{auto} [index, number] : \mbox{\hyperlink{structiterators_1_1impl_1_1ZipView_a4c8c6849391d6b4576cb63ec05a725f3}{enumerate}}(std::array\{53, 21, 17\})) \{}
\DoxyCodeLine{    \textcolor{comment}{// enumerate takes ownership of the array. The elements}}
\DoxyCodeLine{    \textcolor{comment}{// can safely be accessed and manipulated}}
\DoxyCodeLine{\}}

\end{DoxyCode}
 